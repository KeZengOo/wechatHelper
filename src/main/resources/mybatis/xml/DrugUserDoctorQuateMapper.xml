<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nuoxin.virtual.rep.api.mybatis.DrugUserDoctorQuateMapper">
	<insert id="backupRelationShipInfo">
		INSERT INTO drug_user_doctor_quate_log 
		(drug_user_id,doctor_id,product_id, is_has_drug,is_target,is_has_ae,is_recruit, is_break_off)
		
		SELECT drug_user_id,doctor_id,product_id, is_has_drug,is_target,is_has_ae, is_recruit, is_break_off
		FROM drug_user_doctor_quate
		WHERE  
			drug_user_id = #{virtualDrugUserId} 
			AND doctor_id =  #{doctorId} 
			AND product_id = #{productLineId}
	</insert>	
	
	<!-- 使用 MYSQL 私有语法, 通过 drug_user_id, doctor_id, product_id 创建唯一约束 -->
	<insert id="replaceRelationShipInfo">
		REPLACE INTO drug_user_doctor_quate 
		(drug_user_id,doctor_id,product_id, is_has_drug,is_target,is_has_ae, is_break_off)
		VALUE (#{virtualDrugUserId}, #{doctorId}, #{productLineId}, #{isHasDrug}, #{isTarget}, #{isHasAe}, #{isBreakOff})
	</insert>
	
	<insert id="saveDrugUserDoctorQuates" parameterType="com.nuoxin.virtual.rep.api.entity.v2_5.UpdateVirtualDrugUserDoctorRelationship">
		INSERT INTO drug_user_doctor_quate 
		(drug_user_id,doctor_id,product_id, is_has_drug,
		 is_target,is_has_ae, is_recruit)
		VALUES
		<foreach collection ="list" item="index" separator =",">
			 (#{index.virtualDrugUserId}, #{index.doctorId}, #{index.productLineId}, 
			 #{index.isHasDrug}, #{index.isTarget}, #{index.isHasAe}, #{index.isRecruit})
		</foreach>
	</insert>
	
		
</mapper>