<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nuoxin.virtual.rep.api.mybatis.MessageMapper">

    <select id="getMessageStatistics" parameterType="com.nuoxin.virtual.rep.api.web.controller.request.WorkStationRequestBean"
            resultType="com.nuoxin.virtual.rep.api.web.controller.response.work.TodayStatisticsResponseBean">

        select
		(select count(distinct vm.doctor_id) from virtual_message vm
        inner join virtual_doctor vd
        on vm.doctor_id = vd.id
        inner join virtual_drug_user_doctor vdud
        on vd.id = vdud.virtual_doctor_id
        where
        vdud.product_id = #{productId}
        and vd.drug_user_ids like #{drugUserIdStr}
        and vm.message_type=1
        and date_format(vm.message_time, '%Y-%m-%d') = date_format(NOW(), '%Y-%m-%d')
        and vm.product_id=1) as wechatSum
        ,

        (select count(vm.doctor_id) from virtual_message vm
        inner join virtual_doctor vd
        on vm.doctor_id = vd.id
        inner join virtual_drug_user_doctor vdud
        on vd.id = vdud.virtual_doctor_id
        where
        vdud.product_id = #{productId}
        and vd.drug_user_ids like #{drugUserIdStr}
        and vm.message_type=1
        and date_format(vm.message_time, '%Y-%m-%d') = date_format(NOW(), '%Y-%m-%d')
        and vm.product_id=#{productId}) as wechatCount,

        (select count(distinct vm.doctor_id) from virtual_message vm
        inner join virtual_doctor vd
        on vm.doctor_id = vd.id
        inner join virtual_drug_user_doctor vdud
        on vd.id = vdud.virtual_doctor_id
        where
        vdud.product_id = #{productId}
        and vd.drug_user_ids like #{drugUserIdStr}
        and vm.message_type=2
        and date_format(vm.message_time, '%Y-%m-%d') = date_format(NOW(), '%Y-%m-%d')
        and vm.product_id=#{productId}
        ) as imSum,
         (select count(vm.doctor_id) from virtual_message vm
        inner join virtual_doctor vd
        on vm.doctor_id = vd.id
        inner join virtual_drug_user_doctor vdud
        on vd.id = vdud.virtual_doctor_id
        where
        vdud.product_id = #{productId}
        and vd.drug_user_ids like #{drugUserIdStr}
        and vm.message_type=2
        and date_format(vm.message_time, '%Y-%m-%d') = date_format(NOW(), '%Y-%m-%d')
        and vm.product_id=#{productId}
        ) as imCount


    </select>

</mapper>