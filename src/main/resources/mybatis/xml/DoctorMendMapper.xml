<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nuoxin.virtual.rep.api.mybatis.DoctorMendMapper">

	<insert id="saveDoctorMend" keyProperty="id" useGeneratedKeys="true">
		INSERT INTO doctor_mend
			( address, virtual_doctor_id)
		VALUES  
			( #{address}, #{virtualDoctorId})
	</insert>

	<update id="updateDoctorMend" >
		UPDATE doctor_mend SET address=#{address},fixed_phone=#{fixedPhone} WHERE virtual_doctor_id=#{virtualDoctorId}
	</update>

	<select id="getDoctorMend" resultType="java.lang.Long">
		SELECT id FROM doctor_mend  WHERE virtual_doctor_id=#{virtualDoctorId}
	</select>

	<!-- 新增医生地址 -->
	<insert id="addWechat">
		INSERT doctor_mend(wechat,virtual_doctor_id) VALUES(#{wechat}, #{doctorId})
	</insert>

	<!-- 更新医生地址 -->
	<update id="updateWechat">
		UPDATE doctor_mend SET wechat=#{wechat} WHERE virtual_doctor_id=#{doctorId}
	</update>


	<!-- 新增医生地址 -->
	<insert id="addAddress">
		INSERT doctor_mend(address,virtual_doctor_id) VALUES(#{address}, #{doctorId})
	</insert>


	<!-- 更新医生地址 -->
	<update id="updateAddress">
		UPDATE doctor_mend SET address=#{address} WHERE virtual_doctor_id=#{doctorId}
	</update>

	<!-- 根据医生ID查询总数，校验数据是否存在 -->
	<select id="getCountByDoctorId" resultType="java.lang.Integer">
		SELECT COUNT(*) FROM doctor_mend WHERE virtual_doctor_id=#{doctorId}
	</select>


	<!-- 有微信医生人数 -->
	<select id="hasWechatDoctor" resultType="java.lang.Integer">
		SELECT
			COUNT(DISTINCT d.id)
		FROM
			doctor d
		INNER JOIN drug_user_doctor dud ON d.id = dud.doctor_id
		INNER JOIN drug_user du ON dud.drug_user_id = du.id
		INNER JOIN doctor_mend dm
		ON d.id=dm.virtual_doctor_id
		WHERE dud.is_available=1
		AND dud.prod_id=#{productId}
		AND du.id IN
		<foreach collection="drugUserIdList" item="drugUserId" open="(" close=")" separator=",">
			#{drugUserId}
		</foreach>
		AND dm.wechat IS NOT NULL
		AND dm.wechat!=''

		<if test="startTime!=null and startTime !=''">
			AND DATE_FORMAT(dm.create_time, '%Y-%m-%d') >=DATE_FORMAT(#{startTime}, '%Y-%m-%d')
		</if>

		<if test="endTime!=null and endTime !=''">
			AND DATE_FORMAT(dm.create_time, '%Y-%m-%d') &lt;=DATE_FORMAT(#{endTime}, '%Y-%m-%d')
		</if>

	</select>
	
</mapper>